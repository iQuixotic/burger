<h1> I HAVE Burgerseses !!!</h1>
<br><br>
<img src="localhost:3036/public/assets/img/burger.jpg" alt="#">

<div class="row">
  <div class="col-lg-6">
    <h2>Burgers that have been prepared !!</h2>

    <ul>
      {{#each burgers}} {{#unless devoured}} {{> burgers/burger-block devoured=true}} {{/unless}} {{/each}}
    </ul>
  </div>
  <div class="col-lg-6">

    <h2>Burgers that have been devoured!</h2>

    <ul>
      {{#each burgers}} {{#if devoured}} {{> burgers/burger-block devoured=false}} {{/if}} {{/each}}
    </ul>
  </div>
</div>


<h2>Create your own Burger !!!</h2>
<div class="row">
<div class="col-lg-4"></div>
<form class="create-form">

  <div class="form-group">
    <label for="burgTime">Name your creation:</label>
    <input type="text" id="burgTime" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Do you still have it ?</label>
    <br>
    <input type="radio" name="devoured" value="1" checked> 'I must've already eaten it :'(
    <br>
    <input type="radio" name="devoured" value="0"> Just Made It !!
  </div>
  <button class="btn btn-danger" type="submit">Add Burger</button>
</form>
</div>